services:
  db:
    image: mysql:8.0
    container_name: wigell-travels-ms
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: wigell_travel_db
    ports:
      - "6606:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      -
volumes:
  mysql-data: